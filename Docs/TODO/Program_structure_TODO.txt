PROGRAM STRUCTURE - TODO



11.09.2017
GameplayController:
-Make this class use a Singleton model
-Make it have a static instance theInstance:GameplayController
-Write the static getInstance function, which checks if theInstance is null
or not and initializes it if it is. Then returns it

MenuController:

Controller:
	-Make it so pressing the WASD keys moves the character
	-Make it so the readInput function registers which keys
	are being pressed, and sets booleans to true on those
	keys we care about
	-Write the Controller.setController(controllerType:String)
	method
MainCharacter:

-Write the class MovingMonster
-Write the class LevelObject
-Write the class Vector
	-Write the function scalarMultiply(vector, number)
	-Write the function magnitude
	-Write the static function add(vector, vector)
Mansion_game:
-Write the method loadLevel(levelName:String)
	-Make it so this method loads the scene with the
	given name
	-Make it so this method calls the Controller.setController("gameplayController")
	-Make it so this method is called by...
	-Figure out where this method should be called
-Write the function pause
	-Make it so the pause function calls the Controller.setController("menuController")
	function
-Write the class Level